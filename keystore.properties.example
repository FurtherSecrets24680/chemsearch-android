# This is a template for keystore.properties.
# Keep the real keystore.properties out of Git because it contains signing secrets.

storeFile=../keystores/chemsearch-release.jks
storePassword=yourStorePassword
keyAlias=chemsearch
keyPassword=yourKeyPassword
